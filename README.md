# Hybrid Machine Learning Pipeline for Movie Segmentation and Revenue Prediction
Developed ML pipeline clustering movies (budget, runtime, vote avg) using K-Means &amp; Hierarchical with inertia/silhouette for clusters. Trained neural net (ReLU) to predict revenue with preprocessing, normalization, splits; evaluated via MSE &amp; R2. Showcased EDA, unsupervised learning, deep regression.
